extend layout
block title
  title Make Play Live New Account Registration

block body
    .page-header
      h1 #{title}
    if success === true
      .alert.alert-success.success
        | #{message}
    else
      .alert.alert-error.error
        | #{message}

